import { DestacarpalabraDirective } from './resurce.directive';

describe('DestacarpalabraDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarpalabraDirective();
    expect(directive).toBeTruthy();
  });
});
