import { HeroesDirective } from './heroes.directive';

describe('HeroesDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroesDirective();
    expect(directive).toBeTruthy();
  });
});
